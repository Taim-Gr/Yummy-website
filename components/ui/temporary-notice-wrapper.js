"use client";
import TemporaryNotice from "./temporary-notice";

export default function TemporaryNoticeWrapper({
  demoMessage,
  successMessage,
}) {
  return <>{/* Notices temporarily disabled */}</>;
}
